showstars(10);

function showstars (rows) {
    star = '*';
    holder = '';
    for(i=0;i<rows;i++){
        //console.log(star);
        if (true){
            //holder = holder + star;
            holder += star;
            console.log(holder);
        }
    }
}