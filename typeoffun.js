//performs a task
function greet2(name, lastname) {
    console.log('Hello '+name+ '' +lastname);
}
//calculates a value
function square(number) {
    return number*number;
}
//suqare(2);
let number =square(2); //can use it to initalize a variable
//2 is initialized to number
console.log(square(3));

//2 functions calls
