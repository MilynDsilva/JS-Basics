const numbers = [1,-1,2,3];

// const filtered = numbers.filter(function(value){
//     return value >=0; //if it matches this criteria adds the eles to new 
//     //array and returs the new array
// });
const filtered = numbers.filter(value => value >=0); //if it matches this criteria adds the eles to new 
    //array and returs the new array

console.log(filtered);