//strict equality (both have same type and same value)
console.log(1===1); //true
console.log('1'===1); //false

//lose equality
console.log(1==1); //true 
console.log('1'==1); //true we have string on left so converts  right one to string

console.log('true'==1); //comparing true with true
//lose dont care for type but cares only for value
//mostly we use strict
